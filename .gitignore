# 依賴模組 (Dependency directories)
# 這些檔案非常大，且可以透過 npm install 重新產生，不需要上傳到 GitHub
node_modules/
jspm_packages/

# 記錄檔 (Logs)
# 程式執行時產生的日誌檔案，通常只在本地端除錯使用
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 環境變數設定檔 (Environment variables)
# 這裡通常包含敏感資訊（如密碼、API Key），強烈建議不要上傳
.env
.env.test
.env.production
.env.local
.env.development.local
.env.test.local
.env.production.local

# 編譯產出的檔案 (Build outputs)
# 如果你有使用編譯工具，生成的檔案不需要進入版本控制
dist/
build/
.next/
.out/

# 編輯器暫存檔 (Editor folders/files)
# 不同開發者使用的編輯器產生的設定檔，不應影響他人
.vscode/
.idea/
*.swp
*.swo

# 系統垃圾檔案 (System files)
# 操作系統自動產生的檔案
.DS_Store
Thumbs.db
